#include <stdio.h>

/**
 * main - Entry point of the program.
 *
 * Description:
 * This program prints the lowercase
 *	alphabet in reverse order,
 * starting from 'z' and ending with 'a',
 *	followed by a new line.
 *
 * Return: Always 0 (Success)
 */

int main(void)
{
	int i;
	char ch;

	i = 0;
	ch = 122;
	while (i < 26)
	{
		putchar(c);
		c--;
		i++;
	}
	putchar('\n');
	return (0);
}
